# Brandi — Feedback & Instructions

Add notes here to guide Brandi's behavior. She reads this file at the start of every run.
Format: date, then instruction. Brandi will follow the most recent guidance.

---

## Active Instructions

### 2026-08-18 — Post-outage catch-up (TEMPORARY — remove after ~1 week of normal runs)
Gmail access was down June–August 2026, so there is a ~2-month email backlog. Until this instruction is removed:
- Only scan emails from the **last 7 days** (`newer_than:7d`). Do NOT process the full backlog.
- For existing deals: update based on the most recent thread activity only. Deal cards may be very stale — read `next_action`/`notes` first as always; Kenny's manual state wins.
- For new inbound found in the last 7 days: create cards normally.
- For anything older that seems important (unanswered inbound, unpaid invoices), add it as a **suggestion** in the run summary instead of creating/updating cards — Kenny will review the backlog manually.
- Expect first runs to find a lot; keep run summaries focused on what's actionable now.

<!-- Add feedback below this line. Example:
### 2026-04-14
- Stop creating deals from newsletter emails (e.g., Morning Brew, The Hustle)
- Acme Corp is a repeat brand — always set is_repeat_brand: true
- If a brand mentions "gifted" or "seeding", skip it — we only track paid deals
-->
